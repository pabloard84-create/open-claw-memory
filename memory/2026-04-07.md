## 2026-04-07
- Pablo quiere reactivar interacción por voz live y dejar de depender de leer/escribir.
- Se descartó Telegram/WhatsApp/Slack como solución de voz live real; sirven como mensajería, no como conversación full-duplex dentro de llamada.
- Se decidió volver a la ruta Meta Ray-Ban + teléfono puente.
- Hardware actual confirmado: Meta glasses conectados a Galaxy Flip.
- No había implementación previa real; se parte prácticamente desde cero.
- Se validó en el Mac mini que OpenClaw gateway está activo, `/health` responde OK, y `/v1/chat/completions` está habilitado.
- Hallazgo clave: para el endpoint OpenAI-compatible del gateway, el modelo correcto es `openclaw`.
- Se identificó repo local `VisionClaw` con soporte Android en `samples/CameraAccessAndroid`.
- Estrategia acordada: probar primero Phone Mode en Galaxy Flip, luego pasar a Meta glasses streaming.
- Pablo confirmó que tiene Meta glasses conectados a un Galaxy Flip y quiere priorizar una experiencia live/en vivo, no seguir escribiendo.
- Se hizo preflight del Mac mini: inicialmente faltaban `adb`, Android SDK y Android Studio; se confirmó que la ruta Android/Meta es válida pero con fricción de setup.
- Pablo instaló Android Studio y Android platform-tools en el Mac mini siguiendo indicaciones en Terminal.
- Se conectó el Galaxy Flip por USB al Mac mini y ADB quedó funcionando; dispositivo detectado como `R5CY93ENFKB`.
- Bloqueo relevante encontrado en Samsung: `USB debugging` estaba bloqueado por `Auto Blocker`; hubo que desactivarlo para habilitar depuración USB.
- Siguiente paso operativo pendiente: terminar de fijar el SDK path en Android Studio (`/Users/pabloagent/Library/Android/sdk`) y completar instalación mínima del Android SDK para poder abrir/compilar `VisionClaw/samples/CameraAccessAndroid`.
